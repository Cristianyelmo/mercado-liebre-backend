<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('partials/head', {title: 'Inicio'}) %>
<body>
	<!-- Header -->
	<%- include('partials/headerNavbar') %>
	<!-- /Header -->
	
	<!-- Banner -->
	<div class="banner"></div>
	<!-- /Banner -->

	<!-- ultima visita -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Basado en tu última visita</h2>
			</div>
			<!-- Show the "visited" products -->
			<div class="flexbox">
			<!-- 	<a href="/products/detail/ "> -->
				<% products.forEach(({price,name,image,discount,id,category})=> { %>
				<% if (category === "visited" &&  toThousand) { %>
					
						
						
						
						
						<%- include('./partials/products',{
							id,
							image,
							discount,
							price,
							name
						}) %> 





					<% } %>
				 
				<% }) %>
			<!-- </a> -->
			</div>
				 
				
				
			</div>
		</div>
	</div>
	<!-- /ultima visita -->

	<!-- Ofertas -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Ofertas</h2>
			</div>
			<!-- Show the "in-sale" products -->
			<div class="flexbox">
				<% products.forEach(({price,name,image,discount,id,category}) => { %>

					<% if (category === "in-sale") { %>
					
						<%- include('./partials/products',{
							id,
							image,
							discount,
							price,
							name
						}) %> 

						
					<% } %>
				 
				<% }) %>
				</div>
			</div>
			</div>
		</div>
	</div>
	<!-- /Ofertas -->




	
	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>	
</html>